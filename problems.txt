- Can't access rancher as not a member of the organization
- Can't launch locally as pythia_user doesn't exist despite running migrations
- Issues running migrations due to lack of env file


Gut & Restart as SPMS version 5.0.0@alpha
    - Django 4.2
    - Python 3.11.3
    - Poetry 1.4.2
    - PostgreSQL 15
    - Docker 20.10.24
    - React 18.2.0 (to later be changed to Vue to adhere to dev guide)


backend deps
    - black django djangorestframework django-environ gunicorn psycopg2-binary


create new user 
    - 

frontend steps:
    - npx create-react-app my-app --template @chakra-ui/typescript
    - gut down to index react-app-env in src. Remove import errors in index and gut App to just div returning hi
    - npm i react-router-dom
    - Create router.tsx/Root.tsx in src & config
        - Create components/Root.tsx
            import { Outlet } from "react-router-dom"

            export const Root = () => {
                return (
                    <>
                        <Outlet />
                    </>
                )
            }
        - Creaate base pages - home, users, projects, notfound
        - Create Router.tsx
            import { createBrowserRouter } from "react-router-dom";
            import { Root } from "./components/Root";
            import { Home } from "./routes/Home";
            import { Users } from "./routes/Users";


            export const router = createBrowserRouter([{
                path: "/",
                element: <Root />,
                children: [
                    {
                        path: "",
                        element: <Home />,
                    },
                    {
                        path: "users",
                        element: <Users />,
                    },
                ]
            }]);
    - Configure index.tsx to import RouterProvider instead of App
